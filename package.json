{
  "name": "ian",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "desktop:dev": "npm run dev --workspace @ian/desktop",
    "desktop:build": "npm run build --workspace @ian/desktop",
    "desktop:typecheck": "npm run typecheck --workspace @ian/desktop",
    "desktop:test": "npm run test --workspace @ian/desktop",
    "desktop:acceptance": "npm run desktop:test && npm run desktop:typecheck && npm run desktop:build && cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml",
    "desktop:developer-rhythm-acceptance": "npm run desktop:acceptance && (rg -n \"git diff|stdout|stderr|key_code|keypress|keydown|window_title|window title|document.title|clipboard|screenshot|OCR|read_to_string\" apps/desktop/src apps/desktop/src-tauri/src -S || true)",
    "desktop:tauri": "npm run tauri --workspace @ian/desktop"
  }
}
