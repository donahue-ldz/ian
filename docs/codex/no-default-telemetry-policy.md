# No Default Telemetry Policy

Ian 默认没有遥测。

禁止默认启用：

- analytics endpoint
- 自动崩溃上报
- 自动使用数据上传
- 远端诊断同步
- 静默网络请求

允许的本地诊断：

- 用户手动打开本地诊断开关。
- 用户手动导出低敏诊断摘要。
- 本地日志不得包含 API key、代码正文、私聊内容、剪贴板、窗口标题或终端全文。

未来如需联网诊断，必须新增 SDD，经过权限中心明确授权，并提供关闭和删除路径。
