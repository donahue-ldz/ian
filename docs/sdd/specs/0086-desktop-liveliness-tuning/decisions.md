# 0086 决策记录

## 2026-05-21

- 使用 `0086-desktop-liveliness-tuning`，因为 `0084` 和 `0085` 已被其他 SDD 占用。
- 本轮只做追鼠标和 idle 微动作调优，不改资源包、不改设置 UI、不引入新协议。
- 为了真实桌面体验验证，将追鼠标冷却临时缩短到约 10 秒；后续可根据手感再调。
- 桌面验收必须使用真实 Tauri 桌面壳，浏览器预览只作为补充。
- 桌面壳启动时发现并行变更新增的 `screen_bounds` / `last_user_interaction_ms` 未补到 `ConfigFile -> IanState`，本轮只给旧配置恢复路径填默认值，不新增持久化字段。
