# 0074 决策记录

## 2026-05-21

- 拖动中的“抱起”视觉只放在 React/CSS 层，不改变 Rust 协议。
- 拖动开始和结束仍由 Rust Core 输出动作，避免 React 变成行为大脑。
- 先使用现有 `sparkle_pop` 和 `blush_puff` 特效，不新增资源包字段。
- 桌面模式下，`mouse.drag_end` 的坐标语义调整为实际窗口屏幕坐标；浏览器预览仍使用指针坐标。
- 启动时对保存窗口坐标做可见范围夹取，并把夹取后的坐标同步回运行时状态，避免坏配置反复把 Ian 推到屏幕外或左上角。
