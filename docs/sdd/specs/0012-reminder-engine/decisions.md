# 决策记录: Reminder Engine

## 决策日志

| 日期 | 决策 | 原因 | 影响 |
| --- | --- | --- | --- |
| 2026-05-21 | 0012 只做本地时间提醒，不接工作状态。 | v0.1.x 不应提前进入 Developer Rhythm。 | Git/键盘/窗口感知延后到 v0.2。 |
| 2026-05-21 | 首次 `time.tick` 只建立提醒冷却基线，不立刻弹提醒。 | 避免 Ian 启动后马上打扰用户，保持轻陪伴。 | 提醒需要经过一个冷却周期后才会出现。 |
| 2026-05-21 | Playwright 只验证提醒设置开关，提醒触发由 Rust Runtime 测试覆盖。 | 浏览器 fallback 不是真实 Rust Core，且低频提醒不适合靠等待触发。 | 用户可验收设置开关；提醒输出以 Rust 单测作为主要证据。 |

## 范围变化

实现时补充了一个纯前端 `dragGesture` helper 和窗口位置计算函数，以满足工作区已有测试；不改变 0012 的用户可见范围。

## 延后工作

- 基于键盘节奏的工作提醒
- 日历/Feishu 提醒
- 系统通知
